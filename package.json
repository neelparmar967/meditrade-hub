{
  "name": "meditrade-hub",
  "version": "1.0.0",
  "private": true,
  "description": "Secure B2B pharmaceutical ordering platform for retailers, distributors, companies, sales teams, and admins.",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "build": "npm run build --prefix client",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
